# RouteGuard

simple project to showcase how angular RouteGuard CanActivate.

# APP Usage
the default landin ape is Login, its just a plain page, to simulate login functionality, we have a Set component that stores a character in the locl storage, navigate ```localhost:4200/set``` .
If the localstorage is null ,you cannot access the ```localhost:4200``` which is the dashboard